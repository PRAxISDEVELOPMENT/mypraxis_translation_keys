#
# Validates the translation source and rebuilds generated artifacts.
# PRs are checked for validity and sync.
# Pushes to main regenerate i18n/generated/*.json and commit them when needed.
#

name: Build translations

on:
  pull_request:
    paths:
      - 'i18n/translations.json'
      - 'i18n/namespaces.json'
      - 'i18n/buildTranslations.js'
      - 'i18n/processUpload.js'
      - 'i18n/processUploadInbox.js'
      - 'i18n/upload.schema.json'
      - 'i18n/translations.schema.json'
      - '.github/workflows/buildTranslations.yml'
      - '.github/workflows/processTranslationUploads.yml'
  push:
    branches:
      - main
    paths:
      - 'i18n/translations.json'
      - 'i18n/namespaces.json'
      - 'i18n/buildTranslations.js'
      - 'i18n/processUpload.js'
      - 'i18n/processUploadInbox.js'
      - 'i18n/upload.schema.json'
      - 'i18n/translations.schema.json'
      - '.github/workflows/buildTranslations.yml'
      - '.github/workflows/processTranslationUploads.yml'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: translations-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest

    permissions:
      contents: read

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Check translation tooling syntax
        run: |
          node --check i18n/buildTranslations.js
          node --check i18n/processUpload.js
          node --check i18n/processUploadInbox.js

      - name: Validate translation source structure
        run: node i18n/buildTranslations.js

      - name: Check generator output is stable
        run: node i18n/buildTranslations.js --check

  build:
    if: github.event_name != 'pull_request'
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Check translation tooling syntax
        run: |
          node --check i18n/buildTranslations.js
          node --check i18n/processUpload.js
          node --check i18n/processUploadInbox.js

      - name: Build translations
        run: node i18n/buildTranslations.js

      - name: Commit generated files
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add i18n/generated/*.json

          if git diff --cached --quiet; then
            echo "No changes to commit"
          else
            git commit -m "AUTO: generate locale files"
            git pull --rebase origin main
            git push origin HEAD:main
          fi
